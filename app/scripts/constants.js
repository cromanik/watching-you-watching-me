define([], function() {
  'use strict';

  return {
    ANGLE_THRESHOLD: Math.PI / 40,
    ZONE_THRESHOLD: 15,
    DIRECTIONS: {
      LEFT: 'left',
      CENTER: 'center',
      RIGHT: 'right'
    }
  };
});